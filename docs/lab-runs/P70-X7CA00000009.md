# Lab run — P70 `X7CA00000009`

Date: 2026-09-08

## Instrumented UI

```bash
ANDROID_SERIAL=X7CA00000009 ./gradlew :companion-app:connectedDebugAndroidTest
```

Result: **PASS** — `DashboardIdsTest.dashboardExposesStableResourceIds` on `P70 - 14`.
Does not run MQTT or `RELEASE_GATE`.

## Packages

- Production PCM: `/system/app/PoyntCloudMessaging/PoyntCloudMessaging.apk` v`1.0-N12__-v2-d`
- Companion: `0.6.0-phase6` (reinstalled by `MODE=device`)

## Nightly device gate (`MODE=device`)

```bash
ANDROID_SERIAL=X7CA00000009 MODE=device BUILD_ID=lab-P70-20260908 \
  ./scripts/release/run-nightly-gate.sh
```

Orchestration **ran to completion** on this terminal. Verdict: **FAIL (not release-ready)**.

| Check | Result |
|---|---|
| Production Cloud Messaging APK | PASS |
| Companion Test App | PASS |
| Device / PoyntOS | PASS (P70, serial `X7CA00000009`) |
| IoT Companion Gate | FAIL — full flow stopped at Discover (`IoT disabled by Discover`); token fingerprint `none` |
| Negative suite | PASS (`12 pass / 0 fail / 1 skipped`) |

No raw GD token was present or written. MQTT was not attempted because Discover failed first (stop-on-first-FAIL). A green ship verdict needs mothership Discover `iot_enabled` and a valid GD token on this unit.
